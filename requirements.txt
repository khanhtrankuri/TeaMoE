torch>=2.0.0
numpy>=1.24.0
tqdm>=4.65.0
wandb>=0.15.0

# Note: text_utils module is required for process_libri.py
# You may need to create text_utils.py with normalize_transcript function
# or install it if available as a package
